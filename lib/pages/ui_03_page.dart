import 'package:flutter/material.dart';

class Ui03Page extends StatelessWidget {
  const Ui03Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'UI 03'
        ),
      ),
    );
  }
}
